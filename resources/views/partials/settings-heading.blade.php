<div class="relative mb-8 w-full overflow-hidden rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 p-8 shadow-lg">
    <div class="relative z-10">
        <flux:heading size="xl" level="1" class="text-white">{{ __('Settings') }}</flux:heading>
        <flux:subheading size="lg" class="text-blue-100">{{ __('Manage your profile and account settings') }}
        </flux:subheading>
    </div>

    <!-- Decorative Elements -->
    <div class="absolute top-0 right-0 h-64 w-64 rounded-full bg-blue-500 opacity-20 blur-3xl"></div>
    <div class="absolute bottom-0 left-0 h-64 w-64 rounded-full bg-purple-500 opacity-20 blur-3xl"></div>
</div>