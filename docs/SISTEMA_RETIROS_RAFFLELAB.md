# 💸 Sistema de Retiros - RaffleLab
## Documentación Técnica Completa

**Proyecto:** RaffleLab - Superlative Lottery Platform  
**Versión:** CodeCanyon  
**Framework:** Laravel (PHP 8.3+)  
**Fecha de documentación:** Febrero 2026

---

## 📑 Índice

1. [Resumen General](#1-resumen-general)
2. [Arquitectura del Sistema](#2-arquitectura-del-sistema)
3. [Flujo de Retiro](#3-flujo-de-retiro)
4. [Componentes del Sistema](#4-componentes-del-sistema)
5. [Base de Datos](#5-base-de-datos)
6. [Formulario de Retiro (Frontend)](#6-formulario-de-retiro-frontend)
7. [Controladores](#7-controladores)
8. [Panel de Administración](#8-panel-de-administración)
9. [Estados del Retiro](#9-estados-del-retiro)
10. [Configuración y Personalización](#10-configuración-y-personalización)

---

## 1. Resumen General

El sistema de retiros de RaffleLab permite a los usuarios solicitar el retiro de fondos de su balance. A diferencia del sistema de depósitos (que puede ser automático), **todos los retiros son manuales** y requieren aprobación del administrador.

### Características Principales

- **Métodos de retiro configurables** por el administrador
- **Sistema de comisiones** (fijas y porcentuales)
- **Límites de retiro** por método
- **Verificación KYC** opcional antes de retirar
- **Autenticación 2FA** opcional para confirmar retiros
- **Formularios dinámicos** para información del usuario
- **Sistema de aprobación/rechazo** por administrador
- **Reembolso automático** en caso de rechazo

### URL de Acceso
```
https://tu-sitio.com/payout
```

---

## 2. Arquitectura del Sistema

### 2.1 Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CAPA DE PRESENTACIÓN                               │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌─────────────────────┐    ┌─────────────────────┐    ┌──────────────────┐   │
│   │  methods.blade.php  │    │  preview.blade.php  │    │   log.blade.php  │   │
│   │   (Formulario)      │    │   (Confirmación)    │    │   (Historial)    │   │
│   └──────────┬──────────┘    └──────────┬──────────┘    └────────┬─────────┘   │
│              │                          │                         │             │
└──────────────┼──────────────────────────┼─────────────────────────┼─────────────┘
               │                          │                         │
               ▼                          ▼                         ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CAPA DE CONTROLADORES                              │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                    WithdrawController.php (Usuario)                     │   │
│   │  ┌──────────────┐ ┌──────────────┐ ┌───────────────┐ ┌──────────────┐   │   │
│   │  │withdrawMoney │ │withdrawStore │ │withdrawPreview│ │withdrawSubmit│   │   │
│   │  └──────────────┘ └──────────────┘ └───────────────┘ └──────────────┘   │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                   WithdrawalController.php (Admin)                      │   │
│   │  ┌────────────┐ ┌───────────┐ ┌──────────┐ ┌─────────┐ ┌────────────┐   │   │
│   │  │  pending   │ │  approved │ │ rejected │ │ details │ │approve/rej │   │   │
│   │  └────────────┘ └───────────┘ └──────────┘ └─────────┘ └────────────┘   │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
               │                          │                         │
               ▼                          ▼                         ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CAPA DE MODELOS                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌──────────────┐    ┌──────────────────┐    ┌───────────┐    ┌────────────┐  │
│   │  Withdrawal  │◄───│  WithdrawMethod  │    │   User    │    │Transaction │  │
│   │    Model     │    │      Model       │    │   Model   │    │   Model    │  │
│   └──────────────┘    └──────────────────┘    └───────────┘    └────────────┘  │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
               │                          │                         │
               ▼                          ▼                         ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              BASE DE DATOS (MySQL)                              │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌──────────────┐    ┌──────────────────┐    ┌───────────┐    ┌────────────┐  │
│   │ withdrawals  │    │ withdraw_methods │    │   users   │    │transactions│  │
│   └──────────────┘    └──────────────────┘    └───────────┘    └────────────┘  │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 Estructura de Archivos

```
core/
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       ├── User/
│   │       │   └── WithdrawController.php     ← Controlador usuario
│   │       └── Admin/
│   │           ├── WithdrawalController.php   ← Controlador admin
│   │           └── WithdrawMethodController.php
│   │
│   └── Models/
│       ├── Withdrawal.php                     ← Modelo de retiros
│       └── WithdrawMethod.php                 ← Modelo de métodos
│
├── resources/
│   └── views/
│       └── templates/
│           └── basic/
│               └── user/
│                   └── withdraw/
│                       ├── methods.blade.php  ← Formulario principal
│                       ├── preview.blade.php  ← Confirmación
│                       └── log.blade.php      ← Historial
│
└── routes/
    ├── user.php                               ← Rutas usuario
    └── admin.php                              ← Rutas admin
```

---

## 3. Flujo de Retiro

### 3.1 Diagrama de Flujo Completo

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         FLUJO DE RETIRO COMPLETO                             │
└──────────────────────────────────────────────────────────────────────────────┘

     USUARIO                      SISTEMA                        ADMIN
        │                            │                             │
        │  1. Accede a /payout       │                             │
        │───────────────────────────▶│                             │
        │                            │                             │
        │  2. Muestra métodos        │                             │
        │◀───────────────────────────│                             │
        │                            │                             │
        │  3. Selecciona método      │                             │
        │     e ingresa monto        │                             │
        │───────────────────────────▶│                             │
        │                            │                             │
        │                            │  4. Valida:                 │
        │                            │     - Límites               │
        │                            │     - Balance suficiente    │
        │                            │  5. Calcula comisiones      │
        │                            │  6. Crea Withdrawal         │
        │                            │     (status=INITIATE)       │
        │                            │                             │
        │  7. Redirige a /preview    │                             │
        │◀───────────────────────────│                             │
        │                            │                             │
        │  8. Usuario completa       │                             │
        │     información requerida  │                             │
        │     (+ 2FA si está activo) │                             │
        │───────────────────────────▶│                             │
        │                            │                             │
        │                            │  9. Valida info             │
        │                            │ 10. DESCUENTA BALANCE       │
        │                            │     user.balance -= amount  │
        │                            │ 11. Crea Transaction (-)    │
        │                            │ 12. status = PENDING        │
        │                            │ 13. Notifica admin          │
        │                            │────────────────────────────▶│
        │                            │                             │
        │  14. Confirmación          │                             │
        │◀───────────────────────────│                             │
        │                            │                             │
        │       ... ADMIN REVISA LA SOLICITUD ...                  │
        │                            │                             │
        │                            │            ┌────────────────┤
        │                            │            │  15. APRUEBA   │
        │                            │            └───────┬────────┤
        │                            │◀───────────────────┘        │
        │                            │                             │
        │                            │ 16. status = SUCCESS        │
        │                            │ 17. Notifica usuario        │
        │                            │                             │
        │  18. Notificación          │                             │
        │      de aprobación         │                             │
        │◀───────────────────────────│                             │
        │                            │                             │
        ├────────────────────────────┼─────────────────────────────┤
        │     FLUJO ALTERNATIVO: RECHAZO                           │
        ├────────────────────────────┼─────────────────────────────┤
        │                            │                             │
        │                            │            ┌────────────────┤
        │                            │            │  15. RECHAZA   │
        │                            │            └───────┬────────┤
        │                            │◀───────────────────┘        │
        │                            │                             │
        │                            │ 16. status = REJECT         │
        │                            │ 17. REEMBOLSA BALANCE       │
        │                            │     user.balance += amount  │
        │                            │ 18. Crea Transaction (+)    │
        │                            │     (withdraw_reject)       │
        │                            │ 19. Notifica usuario        │
        │                            │                             │
        │  20. Notificación          │                             │
        │      de rechazo            │                             │
        │◀───────────────────────────│                             │
        │                            │                             │
        ▼                            ▼                             ▼
```

### 3.2 Flujo en Código

```
Paso 1: GET /payout
    └── WithdrawController@withdrawMoney()
        └── return view('methods', compact('withdrawMethod'))

Paso 2: POST /payout
    └── WithdrawController@withdrawStore()
        ├── Validar: method_code, amount
        ├── Verificar: límites, balance
        ├── Calcular: charge = fixed + (amount * percent / 100)
        ├── Calcular: afterCharge = amount - charge
        ├── Calcular: finalAmount = afterCharge * rate
        ├── Crear: new Withdrawal() [status=INITIATE]
        ├── Guardar: session('wtrx', $trx)
        └── Redirect: /payout/preview

Paso 3: GET /payout/preview
    └── WithdrawController@withdrawPreview()
        └── return view('preview', compact('withdraw'))

Paso 4: POST /payout/preview
    └── WithdrawController@withdrawSubmit()
        ├── Validar formulario dinámico
        ├── Verificar 2FA (si activo)
        ├── Verificar balance nuevamente
        ├── withdrawal.status = PENDING
        ├── user.balance -= amount
        ├── Crear Transaction (trx_type = '-')
        ├── Notificar admin
        ├── Notificar usuario
        └── Redirect: /payout/history

Paso 5a: ADMIN APRUEBA
    └── Admin\WithdrawalController@approve()
        ├── withdrawal.status = SUCCESS
        └── Notificar usuario (WITHDRAW_APPROVE)

Paso 5b: ADMIN RECHAZA
    └── Admin\WithdrawalController@reject()
        ├── withdrawal.status = REJECT
        ├── user.balance += amount (REEMBOLSO)
        ├── Crear Transaction (trx_type = '+', remark = 'withdraw_reject')
        └── Notificar usuario (WITHDRAW_REJECT)
```

---

## 4. Componentes del Sistema

### 4.1 Rutas (routes/user.php)

```php
// Withdraw Routes
Route::controller('WithdrawController')
    ->prefix('payout')
    ->name('cashout')
    ->group(function () {
    
        // Rutas protegidas por KYC
        Route::middleware('kyc')->group(function () {
            // GET /payout - Mostrar métodos de retiro
            Route::get('/', 'withdrawMoney');
            
            // POST /payout - Procesar selección de método y monto
            Route::post('/', 'withdrawStore')->name('.money');
            
            // GET /payout/preview - Mostrar vista previa
            Route::get('preview', 'withdrawPreview')->name('.preview');
            
            // POST /payout/preview - Confirmar retiro
            Route::post('preview', 'withdrawSubmit')->name('.submit');
        });
        
        // GET /payout/history - Historial de retiros
        Route::get('history', 'withdrawLog')->name('.history');
    });
```

### 4.2 Modelo Withdrawal

**Archivo:** `app/Models/Withdrawal.php`

```php
<?php

namespace App\Models;

use App\Constants\Status;
use Illuminate\Database\Eloquent\Casts\Attribute;
use Illuminate\Database\Eloquent\Model;

class Withdrawal extends Model
{
    protected $casts = [
        'withdraw_information' => 'object'
    ];

    protected $hidden = [
        'withdraw_information'  // Oculta datos sensibles
    ];

    // ═══════════════════════════════════════════════════════════
    // RELACIONES
    // ═══════════════════════════════════════════════════════════
    
    /**
     * Usuario que solicita el retiro
     */
    public function user()
    {
        return $this->belongsTo(User::class);
    }

    /**
     * Método de retiro utilizado
     */
    public function method()
    {
        return $this->belongsTo(WithdrawMethod::class, 'method_id');
    }

    // ═══════════════════════════════════════════════════════════
    // ATRIBUTOS COMPUTADOS
    // ═══════════════════════════════════════════════════════════
    
    /**
     * Badge HTML según el estado
     */
    public function statusBadge(): Attribute
    {
        return new Attribute(function() {
            switch($this->status) {
                case Status::PAYMENT_PENDING:
                    return '<span class="badge badge--warning">Pending</span>';
                case Status::PAYMENT_SUCCESS:
                    return '<span class="badge badge--success">Approved</span>';
                case Status::PAYMENT_REJECT:
                    return '<span class="badge badge--danger">Rejected</span>';
                default:
                    return '';
            }
        });
    }

    // ═══════════════════════════════════════════════════════════
    // SCOPES (Consultas predefinidas)
    // ═══════════════════════════════════════════════════════════
    
    public function scopePending($query)
    {
        return $query->where('status', Status::PAYMENT_PENDING);
    }

    public function scopeApproved($query)
    {
        return $query->where('status', Status::PAYMENT_SUCCESS);
    }

    public function scopeRejected($query)
    {
        return $query->where('status', Status::PAYMENT_REJECT);
    }
}
```

### 4.3 Modelo WithdrawMethod

**Archivo:** `app/Models/WithdrawMethod.php`

```php
<?php

namespace App\Models;

use App\Traits\GlobalStatus;
use Illuminate\Database\Eloquent\Model;

class WithdrawMethod extends Model
{
    use GlobalStatus;  // Proporciona scope 'active'

    protected $casts = [
        'user_data' => 'object',  // Datos del formulario dinámico
    ];

    // ═══════════════════════════════════════════════════════════
    // RELACIONES
    // ═══════════════════════════════════════════════════════════
    
    /**
     * Formulario dinámico asociado
     */
    public function form()
    {
        return $this->belongsTo(Form::class);
    }
}
```

---

## 5. Base de Datos

### 5.1 Tabla `withdrawals`

```sql
CREATE TABLE `withdrawals` (
    `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
    
    -- Relaciones
    `method_id` int(10) UNSIGNED NOT NULL DEFAULT 0,
    `user_id` int(10) UNSIGNED NOT NULL DEFAULT 0,
    
    -- Montos
    `amount` decimal(28,8) NOT NULL DEFAULT 0.00000000,        -- Monto solicitado
    `charge` decimal(28,8) NOT NULL DEFAULT 0.00000000,        -- Comisión
    `after_charge` decimal(28,8) NOT NULL DEFAULT 0.00000000,  -- Monto después de comisión
    `final_amount` decimal(28,8) NOT NULL DEFAULT 0.00000000,  -- Monto final en moneda destino
    `rate` decimal(28,8) NOT NULL DEFAULT 0.00000000,          -- Tasa de cambio
    
    -- Moneda destino
    `currency` varchar(40) DEFAULT NULL,
    
    -- Tracking
    `trx` varchar(40) DEFAULT NULL,                            -- Código único
    
    -- Información del usuario
    `withdraw_information` text DEFAULT NULL,                  -- JSON con datos del formulario
    
    -- Estado
    `status` tinyint(1) NOT NULL DEFAULT 0,                    -- 0=init, 1=success, 2=pending, 3=reject
    
    -- Feedback del admin
    `admin_feedback` text DEFAULT NULL,
    
    -- Timestamps
    `created_at` timestamp NULL DEFAULT NULL,
    `updated_at` timestamp NULL DEFAULT NULL,
    
    PRIMARY KEY (`id`),
    KEY `withdrawals_user_id_index` (`user_id`),
    KEY `withdrawals_method_id_index` (`method_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 5.2 Tabla `withdraw_methods`

```sql
CREATE TABLE `withdraw_methods` (
    `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
    `form_id` int(10) UNSIGNED NOT NULL DEFAULT 0,    -- FK al formulario dinámico
    `name` varchar(40) DEFAULT NULL,                   -- Nombre del método
    
    -- Límites
    `min_limit` decimal(28,8) DEFAULT 0.00000000,     -- Mínimo a retirar
    `max_limit` decimal(28,8) NOT NULL DEFAULT 0.00000000,  -- Máximo a retirar
    
    -- Comisiones
    `fixed_charge` decimal(28,8) DEFAULT 0.00000000,  -- Comisión fija
    `percent_charge` decimal(5,2) DEFAULT NULL,       -- Comisión porcentual
    
    -- Conversión
    `rate` decimal(28,8) DEFAULT 0.00000000,          -- Tasa de cambio
    `currency` varchar(40) DEFAULT NULL,              -- Moneda de destino
    
    -- Información
    `description` text DEFAULT NULL,                   -- Instrucciones para el usuario
    
    -- Estado
    `status` tinyint(1) NOT NULL DEFAULT 1,           -- Activo/Inactivo
    
    -- Timestamps
    `created_at` timestamp NULL DEFAULT NULL,
    `updated_at` timestamp NULL DEFAULT NULL,
    
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 5.3 Diagrama Entidad-Relación

```
┌─────────────────┐       ┌─────────────────────┐       ┌──────────────────┐
│     users       │       │     withdrawals     │       │ withdraw_methods │
├─────────────────┤       ├─────────────────────┤       ├──────────────────┤
│ PK id           │◄──────│ FK user_id          │       │ PK id            │
│    username     │       │ PK id               │       │    form_id       │
│    email        │       │ FK method_id        │──────▶│    name          │
│    balance      │       │    amount           │       │    min_limit     │
│    ...          │       │    charge           │       │    max_limit     │
└─────────────────┘       │    after_charge     │       │    fixed_charge  │
                          │    final_amount     │       │    percent_charge│
                          │    rate             │       │    rate          │
                          │    currency         │       │    currency      │
                          │    trx              │       │    description   │
                          │    withdraw_info    │       │    status        │
                          │    status           │       │    ...           │
                          │    admin_feedback   │       └──────────────────┘
                          │    ...              │
                          └─────────────────────┘
                                    │
                                    │ (Transacción creada
                                    │  al enviar solicitud)
                                    ▼
                          ┌─────────────────────┐
                          │    transactions     │
                          ├─────────────────────┤
                          │ PK id               │
                          │ FK user_id          │
                          │    amount           │
                          │    post_balance     │
                          │    charge           │
                          │    trx_type ('-')   │
                          │    trx              │
                          │    remark           │
                          │    ...              │
                          └─────────────────────┘
```

---

## 6. Formulario de Retiro (Frontend)

### 6.1 Estructura Visual - Paso 1: Selección de Método

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                                                                 │
│   Withdraw                                                   [ ← History ]      │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌─────────────────────────────────┐   ┌─────────────────────────────────────┐ │
│   │                                 │   │                                     │ │
│   │   MÉTODOS DE RETIRO             │   │   INFORMACIÓN DEL RETIRO            │ │
│   │   ═══════════════════════════   │   │   ═══════════════════════════════   │ │
│   │                                 │   │                                     │ │
│   │   ┌─────────────────────────┐   │   │   Amount                            │ │
│   │   │ ○ Bank Transfer [logo]  │   │   │   ┌─────────────────────────────┐   │ │
│   │   └─────────────────────────┘   │   │   │  $  │  [_______________]    │   │ │
│   │   ┌─────────────────────────┐   │   │   └─────────────────────────────┘   │ │
│   │   │ ● PayPal        [logo]  │   │   │                                     │ │
│   │   └─────────────────────────┘   │   │   ───────────────────────────────   │ │
│   │   ┌─────────────────────────┐   │   │                                     │ │
│   │   │ ○ Bitcoin       [logo]  │   │   │   Limit:              $50 - $5000   │ │
│   │   └─────────────────────────┘   │   │                                     │ │
│   │   ┌─────────────────────────┐   │   │   Processing Charge:  $3.00 USD     │ │
│   │   │ ○ Western Union [logo]  │   │   │   ℹ️ 1.5% + $1.50 fixed             │ │
│   │   └─────────────────────────┘   │   │                                     │ │
│   │                                 │   │   ───────────────────────────────   │ │
│   │   [  Show All Payment Options ] │   │                                     │ │
│   │                                 │   │   Receivable:         $97.00 USD    │ │
│   │                                 │   │                                     │ │
│   └─────────────────────────────────┘   │   Conversion:   1 USD = 0.92 EUR    │ │
│                                         │   In EUR:             €89.24        │ │
│                                         │                                     │ │
│                                         │   ┌─────────────────────────────┐   │ │
│                                         │   │     Confirm Withdraw        │   │ │
│                                         │   └─────────────────────────────┘   │ │
│                                         │                                     │ │
│                                         │   ℹ️ Safely withdraw your funds     │ │
│                                         │   using our highly secure process   │ │
│                                         │                                     │ │
│                                         └─────────────────────────────────────┘ │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 Estructura Visual - Paso 2: Confirmación

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                                                                 │
│   Withdraw Confirmation                                                         │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                                                                         │   │
│   │   📋 Please provide your PayPal account details to receive funds.       │   │
│   │      Ensure the information is correct to avoid delays.                 │   │
│   │                                                                         │   │
│   │   ─────────────────────────────────────────────────────────────────     │   │
│   │                                                                         │   │
│   │   PayPal Email *                                                        │   │
│   │   ┌─────────────────────────────────────────────────────────────────┐   │   │
│   │   │  user@example.com                                               │   │   │
│   │   └─────────────────────────────────────────────────────────────────┘   │   │
│   │                                                                         │   │
│   │   Account Holder Name *                                                 │   │
│   │   ┌─────────────────────────────────────────────────────────────────┐   │   │
│   │   │  John Doe                                                       │   │   │
│   │   └─────────────────────────────────────────────────────────────────┘   │   │
│   │                                                                         │   │
│   │   Google Authenticator Code (si 2FA está activo)                        │   │
│   │   ┌─────────────────────────────────────────────────────────────────┐   │   │
│   │   │  ______                                                         │   │   │
│   │   └─────────────────────────────────────────────────────────────────┘   │   │
│   │                                                                         │   │
│   │   ┌─────────────────────────────────────────────────────────────────┐   │   │
│   │   │                         Submit                                  │   │   │
│   │   └─────────────────────────────────────────────────────────────────┘   │   │
│   │                                                                         │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 6.3 Código del Formulario Principal

**Archivo:** `resources/views/templates/basic/user/withdraw/methods.blade.php`

```html
@extends($activeTemplate . 'layouts.master')
@section('content')
<div class="dashboard-inner">
    <div class="row justify-content-center">
        <div class="col-md-12">
            
            <!-- Header -->
            <div class="d-flex align-items-center justify-content-between mb-3">
                <h4>{{ __(withrdawKeyword()) }}</h4>
                <a class="btn btn--secondary" href="{{ route('user.cashout.history') }}">
                    <i class="las la-long-arrow-alt-left"></i> @lang('History')
                </a>
            </div>
            
            <!-- Formulario -->
            <form action="{{ route('user.cashout.money') }}" method="post" class="withdraw-form">
                @csrf
                <div class="gateway-card">
                    <div class="row justify-content-center gy-sm-4 gy-3">
                        
                        <!-- Lista de métodos -->
                        <div class="col-lg-6">
                            <div class="payment-system-list gateway-option-list">
                                @foreach ($withdrawMethod as $data)
                                <label for="{{ titleToKey($data->name) }}" 
                                       class="payment-item gateway-option">
                                    <div class="payment-item__info">
                                        <span class="payment-item__check"></span>
                                        <span class="payment-item__name">{{ __($data->name) }}</span>
                                    </div>
                                    <div class="payment-item__thumb">
                                        <img src="{{ getImage(getFilePath('withdrawMethod').'/'.$data->image) }}"
                                             alt="@lang('payment-thumb')">
                                    </div>
                                    <input class="payment-item__radio gateway-input" 
                                           type="radio" 
                                           name="method_code" 
                                           value="{{ $data->id }}"
                                           data-gateway='@json($data)'
                                           data-min-amount="{{ showAmount($data->min_limit) }}"
                                           data-max-amount="{{ showAmount($data->max_limit) }}"
                                           hidden
                                           @checked($loop->first)>
                                </label>
                                @endforeach
                            </div>
                        </div>
                        
                        <!-- Información del retiro -->
                        <div class="col-lg-6">
                            <div class="payment-system-list p-3">
                                <!-- Campo de monto -->
                                <div class="deposit-info">
                                    <p class="text mb-0">@lang('Amount')</p>
                                    <div class="input-group">
                                        <span class="input-group-text">{{ gs('cur_sym') }}</span>
                                        <input type="text" class="form-control amount" 
                                               name="amount" placeholder="00.00">
                                    </div>
                                </div>
                                
                                <hr>
                                
                                <!-- Límites -->
                                <div class="deposit-info">
                                    <p class="text">@lang('Limit')</p>
                                    <p class="text"><span class="gateway-limit">0.00</span></p>
                                </div>
                                
                                <!-- Comisión -->
                                <div class="deposit-info">
                                    <p class="text">@lang('Processing Charge')</p>
                                    <p class="text">
                                        {{ gs('cur_sym') }}<span class="processing-fee">0.00</span>
                                    </p>
                                </div>
                                
                                <!-- Monto a recibir -->
                                <div class="deposit-info total-amount pt-3">
                                    <p class="text">@lang('Receivable')</p>
                                    <p class="text">
                                        {{ gs('cur_sym') }}<span class="final-amount">0.00</span>
                                    </p>
                                </div>
                                
                                <!-- Conversión -->
                                <div class="deposit-info gateway-conversion d-none">
                                    <p class="text">@lang('Conversion')</p>
                                    <p class="text"></p>
                                </div>
                                
                                <!-- Botón -->
                                <div class="mt-3">
                                    <button type="submit" class="btn btn--base w-100" disabled>
                                        @lang('Confirm Withdraw')
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
@endsection
```

### 6.4 JavaScript - Cálculo de Montos

```javascript
"use strict";
(function($) {
    
    var amount = parseFloat($('.amount').val() || 0);
    var gateway, minAmount, maxAmount;
    
    // ═══════════════════════════════════════════════════════════════════
    // EVENT: Cambio en el monto
    // ═══════════════════════════════════════════════════════════════════
    $('.amount').on('input', function(e) {
        amount = parseFloat($(this).val()) || 0;
        calculation();
    });
    
    // ═══════════════════════════════════════════════════════════════════
    // EVENT: Cambio de método
    // ═══════════════════════════════════════════════════════════════════
    $('.gateway-input').on('change', function(e) {
        gatewayChange();
    });
    
    function gatewayChange() {
        let gatewayElement = $('.gateway-input:checked');
        
        gateway = gatewayElement.data('gateway');
        minAmount = gatewayElement.data('min-amount');
        maxAmount = gatewayElement.data('max-amount');
        
        calculation();
    }
    
    // ═══════════════════════════════════════════════════════════════════
    // FUNCIÓN: Cálculo de montos (DIFERENTE A DEPÓSITOS)
    // ═══════════════════════════════════════════════════════════════════
    function calculation() {
        if (!gateway) return;
        
        $(".gateway-limit").text(minAmount + " - " + maxAmount);
        
        // Calcular comisiones
        let percentCharge = parseFloat(gateway.percent_charge) || 0;
        let fixedCharge = parseFloat(gateway.fixed_charge) || 0;
        let totalPercentCharge = amount / 100 * percentCharge;
        let totalCharge = totalPercentCharge + fixedCharge;
        
        // IMPORTANTE: En retiros se RESTA la comisión
        let totalAmount = amount - totalCharge;
        
        $(".final-amount").text(totalAmount.toFixed(2));
        $(".processing-fee").text(totalCharge.toFixed(2));
        $(".gateway-currency").text(gateway.currency);
        
        // Validar límites
        if (amount < Number(gateway.min_limit) || amount > Number(gateway.max_limit)) {
            $(".withdraw-form button[type=submit]").attr('disabled', true);
        } else {
            $(".withdraw-form button[type=submit]").removeAttr('disabled');
        }
        
        // Mostrar conversión si es diferente moneda
        if (gateway.currency != "{{ gs('cur_text') }}") {
            $(".gateway-conversion").removeClass('d-none');
            $(".gateway-conversion .text:last").html(
                `1 {{ gs('cur_text') }} = ${gateway.rate} ${gateway.currency}`
            );
            $('.in-currency').text((totalAmount * gateway.rate).toFixed(2));
        } else {
            $(".gateway-conversion").addClass('d-none');
        }
    }
    
    // Inicializar
    gatewayChange();
    
})(jQuery);
```

---

## 7. Controladores

### 7.1 WithdrawController (Usuario)

**Archivo:** `app/Http/Controllers/User/WithdrawController.php`

```php
<?php

namespace App\Http\Controllers\User;

use App\Constants\Status;
use App\Http\Controllers\Controller;
use App\Lib\FormProcessor;
use App\Models\AdminNotification;
use App\Models\Transaction;
use App\Models\Withdrawal;
use App\Models\WithdrawMethod;
use Illuminate\Http\Request;

class WithdrawController extends Controller
{
    // ═══════════════════════════════════════════════════════════════════════════
    // PASO 1: Mostrar métodos de retiro
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function withdrawMoney()
    {
        $withdrawMethod = WithdrawMethod::active()->get();
        $pageTitle = 'Withdraw Money';
        return view('Template::user.withdraw.methods', compact('pageTitle', 'withdrawMethod'));
    }

    // ═══════════════════════════════════════════════════════════════════════════
    // PASO 2: Validar y crear registro de retiro
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function withdrawStore(Request $request)
    {
        $request->validate([
            'method_code' => 'required',
            'amount' => 'required|numeric'
        ]);
        
        $method = WithdrawMethod::where('id', $request->method_code)
            ->active()
            ->firstOrFail();
        $user = auth()->user();
        
        // ═══════════════════════════════════════════════════════════════════
        // VALIDACIONES
        // ═══════════════════════════════════════════════════════════════════
        
        // Validar monto mínimo
        if ($request->amount < $method->min_limit) {
            $notify[] = ['error', 'Your requested amount is smaller than minimum amount'];
            return back()->withNotify($notify)->withInput();
        }
        
        // Validar monto máximo
        if ($request->amount > $method->max_limit) {
            $notify[] = ['error', 'Your requested amount is larger than maximum amount'];
            return back()->withNotify($notify)->withInput();
        }
        
        // Validar balance suficiente
        if ($request->amount > $user->balance) {
            $notify[] = ['error', 'Insufficient balance for withdrawal'];
            return back()->withNotify($notify)->withInput();
        }

        // ═══════════════════════════════════════════════════════════════════
        // CÁLCULO DE COMISIONES (DIFERENTE A DEPÓSITOS)
        // En retiros: se RESTA la comisión del monto
        // ═══════════════════════════════════════════════════════════════════
        
        $charge = $method->fixed_charge + ($request->amount * $method->percent_charge / 100);
        $afterCharge = $request->amount - $charge;  // Monto después de comisión
        
        // Validar que quede algo después de las comisiones
        if ($afterCharge <= 0) {
            $notify[] = ['error', 'Withdraw amount must be sufficient for charges'];
            return back()->withNotify($notify)->withInput();
        }
        
        $finalAmount = $afterCharge * $method->rate;  // Convertir a moneda destino

        // ═══════════════════════════════════════════════════════════════════
        // CREAR REGISTRO DE RETIRO
        // ═══════════════════════════════════════════════════════════════════
        
        $withdraw = new Withdrawal();
        $withdraw->method_id = $method->id;
        $withdraw->user_id = $user->id;
        $withdraw->amount = $request->amount;
        $withdraw->currency = $method->currency;
        $withdraw->rate = $method->rate;
        $withdraw->charge = $charge;
        $withdraw->final_amount = $finalAmount;
        $withdraw->after_charge = $afterCharge;
        $withdraw->trx = getTrx();
        $withdraw->save();
        
        session()->put('wtrx', $withdraw->trx);
        return to_route('user.cashout.preview');
    }

    // ═══════════════════════════════════════════════════════════════════════════
    // PASO 3: Vista previa del retiro
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function withdrawPreview()
    {
        $withdraw = Withdrawal::with('method', 'user')
            ->where('trx', session()->get('wtrx'))
            ->where('status', Status::PAYMENT_INITIATE)
            ->orderBy('id', 'desc')
            ->firstOrFail();
            
        $pageTitle = 'Withdraw Preview';
        return view('Template::user.withdraw.preview', compact('pageTitle', 'withdraw'));
    }

    // ═══════════════════════════════════════════════════════════════════════════
    // PASO 4: Confirmar y enviar solicitud de retiro
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function withdrawSubmit(Request $request)
    {
        $withdraw = Withdrawal::with('method', 'user')
            ->where('trx', session()->get('wtrx'))
            ->where('status', Status::PAYMENT_INITIATE)
            ->orderBy('id', 'desc')
            ->firstOrFail();

        $method = $withdraw->method;
        if ($method->status == Status::DISABLE) {
            abort(404);
        }

        // Procesar formulario dinámico
        $formData = @$method->form->form_data ?? [];
        $formProcessor = new FormProcessor();
        $validationRule = $formProcessor->valueValidation($formData);
        $request->validate($validationRule);
        $userData = $formProcessor->processFormData($request, $formData);

        // ═══════════════════════════════════════════════════════════════════
        // VERIFICAR 2FA (si está activo)
        // ═══════════════════════════════════════════════════════════════════
        
        $user = auth()->user();
        if ($user->ts) {
            $response = verifyG2fa($user, $request->authenticator_code);
            if (!$response) {
                $notify[] = ['error', 'Wrong verification code'];
                return back()->withNotify($notify)->withInput();
            }
        }

        // Verificar balance nuevamente (podría haber cambiado)
        if ($withdraw->amount > $user->balance) {
            $notify[] = ['error', 'Your request amount is larger than your current balance'];
            return back()->withNotify($notify)->withInput();
        }

        // ═══════════════════════════════════════════════════════════════════
        // ACTUALIZAR ESTADO Y DESCONTAR BALANCE
        // ═══════════════════════════════════════════════════════════════════
        
        $withdraw->status = Status::PAYMENT_PENDING;
        $withdraw->withdraw_information = $userData;
        $withdraw->save();
        
        // IMPORTANTE: Descontar del balance inmediatamente
        $user->balance -= $withdraw->amount;
        $user->save();

        // ═══════════════════════════════════════════════════════════════════
        // CREAR TRANSACCIÓN
        // ═══════════════════════════════════════════════════════════════════
        
        $transaction = new Transaction();
        $transaction->user_id = $withdraw->user_id;
        $transaction->amount = $withdraw->amount;
        $transaction->post_balance = $user->balance;
        $transaction->charge = $withdraw->charge;
        $transaction->trx_type = '-';  // Tipo NEGATIVO
        $transaction->details = 'Withdraw request via ' . $withdraw->method->name;
        $transaction->trx = $withdraw->trx;
        $transaction->remark = 'withdraw';
        $transaction->save();

        // ═══════════════════════════════════════════════════════════════════
        // NOTIFICACIONES
        // ═══════════════════════════════════════════════════════════════════
        
        // Notificar al administrador
        $adminNotification = new AdminNotification();
        $adminNotification->user_id = $user->id;
        $adminNotification->title = 'New withdraw request from ' . $user->username;
        $adminNotification->click_url = urlPath('admin.withdraw.data.details', $withdraw->id);
        $adminNotification->save();

        // Notificar al usuario
        notify($user, 'WITHDRAW_REQUEST', [
            'method_name' => $withdraw->method->name,
            'method_currency' => $withdraw->currency,
            'method_amount' => showAmount($withdraw->final_amount, currencyFormat: false),
            'amount' => showAmount($withdraw->amount, currencyFormat: false),
            'charge' => showAmount($withdraw->charge, currencyFormat: false),
            'rate' => showAmount($withdraw->rate, currencyFormat: false),
            'trx' => $withdraw->trx,
            'post_balance' => showAmount($user->balance, currencyFormat: false),
        ]);

        $notify[] = ['success', 'Withdraw request sent successfully'];
        return to_route('user.cashout.history')->withNotify($notify);
    }

    // ═══════════════════════════════════════════════════════════════════════════
    // HISTORIAL DE RETIROS
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function withdrawLog(Request $request)
    {
        $pageTitle = "Withdrawal Log";
        $withdraws = Withdrawal::where('user_id', auth()->id())
            ->where('status', '!=', Status::PAYMENT_INITIATE);
            
        if ($request->search) {
            $withdraws = $withdraws->where('trx', $request->search);
        }
        
        $withdraws = $withdraws->with('method')
            ->orderBy('id', 'desc')
            ->paginate(getPaginate());
            
        return view('Template::user.withdraw.log', compact('pageTitle', 'withdraws'));
    }
}
```

---

## 8. Panel de Administración

### 8.1 WithdrawalController (Admin)

**Archivo:** `app/Http/Controllers/Admin/WithdrawalController.php`

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Constants\Status;
use App\Http\Controllers\Controller;
use App\Models\Transaction;
use App\Models\Withdrawal;
use Illuminate\Http\Request;

class WithdrawalController extends Controller
{
    // ═══════════════════════════════════════════════════════════════════════════
    // LISTADOS
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function pending($userId = null)
    {
        $pageTitle = 'Pending Withdrawals';
        $withdrawals = $this->withdrawalData('pending', userId: $userId);
        return view('admin.withdraw.withdrawals', compact('pageTitle', 'withdrawals'));
    }

    public function approved($userId = null)
    {
        $pageTitle = 'Approved Withdrawals';
        $withdrawals = $this->withdrawalData('approved', userId: $userId);
        return view('admin.withdraw.withdrawals', compact('pageTitle', 'withdrawals'));
    }

    public function rejected($userId = null)
    {
        $pageTitle = 'Rejected Withdrawals';
        $withdrawals = $this->withdrawalData('rejected', userId: $userId);
        return view('admin.withdraw.withdrawals', compact('pageTitle', 'withdrawals'));
    }

    public function all($userId = null)
    {
        $pageTitle = 'All Withdrawals';
        $withdrawalData = $this->withdrawalData(null, summary: true, userId: $userId);
        // ... retorna vista con resumen
    }

    // ═══════════════════════════════════════════════════════════════════════════
    // DETALLES DE UN RETIRO
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function details($id)
    {
        $withdrawal = Withdrawal::where('id', $id)
            ->where('status', '!=', Status::PAYMENT_INITIATE)
            ->with(['user', 'method'])
            ->firstOrFail();
            
        $pageTitle = $withdrawal->user->username . ' Withdraw Requested ' . showAmount($withdrawal->amount);
        $details = $withdrawal->withdraw_information 
            ? json_encode($withdrawal->withdraw_information) 
            : null;

        return view('admin.withdraw.detail', compact('pageTitle', 'withdrawal', 'details'));
    }

    // ═══════════════════════════════════════════════════════════════════════════
    // APROBAR RETIRO
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function approve(Request $request)
    {
        $request->validate(['id' => 'required|integer']);
        
        $withdraw = Withdrawal::where('id', $request->id)
            ->where('status', Status::PAYMENT_PENDING)
            ->with('user')
            ->firstOrFail();
        
        // Cambiar estado a SUCCESS
        $withdraw->status = Status::PAYMENT_SUCCESS;
        $withdraw->admin_feedback = $request->details;
        $withdraw->save();

        // Notificar al usuario
        notify($withdraw->user, 'WITHDRAW_APPROVE', [
            'method_name' => $withdraw->method->name,
            'method_currency' => $withdraw->currency,
            'method_amount' => showAmount($withdraw->final_amount, currencyFormat: false),
            'amount' => showAmount($withdraw->amount, currencyFormat: false),
            'charge' => showAmount($withdraw->charge, currencyFormat: false),
            'rate' => showAmount($withdraw->rate, currencyFormat: false),
            'trx' => $withdraw->trx,
            'admin_details' => $request->details
        ]);

        $notify[] = ['success', 'Withdrawal approved successfully'];
        return to_route('admin.withdraw.data.pending')->withNotify($notify);
    }

    // ═══════════════════════════════════════════════════════════════════════════
    // RECHAZAR RETIRO (CON REEMBOLSO AUTOMÁTICO)
    // ═══════════════════════════════════════════════════════════════════════════
    
    public function reject(Request $request)
    {
        $request->validate(['id' => 'required|integer']);
        
        $withdraw = Withdrawal::where('id', $request->id)
            ->where('status', Status::PAYMENT_PENDING)
            ->with('user')
            ->firstOrFail();

        // Cambiar estado a REJECT
        $withdraw->status = Status::PAYMENT_REJECT;
        $withdraw->admin_feedback = $request->details;
        $withdraw->save();

        // ═══════════════════════════════════════════════════════════════════
        // REEMBOLSAR EL BALANCE AL USUARIO
        // ═══════════════════════════════════════════════════════════════════
        
        $user = $withdraw->user;
        $user->balance += $withdraw->amount;  // Devolver el monto completo
        $user->save();

        // Crear transacción de reembolso
        $transaction = new Transaction();
        $transaction->user_id = $withdraw->user_id;
        $transaction->amount = $withdraw->amount;
        $transaction->post_balance = $user->balance;
        $transaction->charge = 0;
        $transaction->trx_type = '+';  // Tipo POSITIVO (reembolso)
        $transaction->remark = 'withdraw_reject';
        $transaction->details = 'Refunded for withdrawal rejection';
        $transaction->trx = $withdraw->trx;
        $transaction->save();

        // Notificar al usuario
        notify($user, 'WITHDRAW_REJECT', [
            'method_name' => $withdraw->method->name,
            'method_currency' => $withdraw->currency,
            'method_amount' => showAmount($withdraw->final_amount, currencyFormat: false),
            'amount' => showAmount($withdraw->amount, currencyFormat: false),
            'charge' => showAmount($withdraw->charge, currencyFormat: false),
            'rate' => showAmount($withdraw->rate, currencyFormat: false),
            'trx' => $withdraw->trx,
            'post_balance' => showAmount($user->balance, currencyFormat: false),
            'admin_details' => $request->details
        ]);

        $notify[] = ['success', 'Withdrawal rejected successfully'];
        return to_route('admin.withdraw.data.pending')->withNotify($notify);
    }
}
```

---

## 9. Estados del Retiro

### 9.1 Diagrama de Estados

```
                                    ┌─────────────────┐
                                    │     INICIO      │
                                    └────────┬────────┘
                                             │
                                             ▼
                               ┌─────────────────────────┐
                               │   PAYMENT_INITIATE (0)  │
                               │   "Retiro Iniciado"     │
                               │                         │
                               │ • Registro creado       │
                               │ • Balance NO afectado   │
                               └─────────────┬───────────┘
                                             │
                                             │ Usuario confirma
                                             │ (withdrawSubmit)
                                             ▼
                               ┌─────────────────────────┐
                               │   PAYMENT_PENDING (2)   │
                               │   "Pendiente Revisión"  │
                               │                         │
                               │ • Balance DESCONTADO    │
                               │ • Transaction creada (-) │
                               │ • Admin notificado      │
                               └─────────────┬───────────┘
                                             │
                              ┌──────────────┴──────────────┐
                              │                             │
                              ▼                             ▼
                    ┌─────────────────┐           ┌─────────────────┐
                    │  Admin APRUEBA  │           │  Admin RECHAZA  │
                    └────────┬────────┘           └────────┬────────┘
                             │                             │
                             ▼                             ▼
               ┌─────────────────────────┐   ┌─────────────────────────┐
               │  PAYMENT_SUCCESS (1)    │   │   PAYMENT_REJECT (3)    │
               │  "Retiro Aprobado"      │   │   "Retiro Rechazado"    │
               │                         │   │                         │
               │ • Usuario notificado    │   │ • Balance REEMBOLSADO   │
               │ • Admin procesa pago    │   │ • Transaction (+)       │
               │   manualmente           │   │ • Usuario notificado    │
               └─────────────────────────┘   └─────────────────────────┘
```

### 9.2 Tabla de Estados

| Código | Constante | Descripción | Balance | Acción Admin |
|--------|-----------|-------------|---------|--------------|
| 0 | `PAYMENT_INITIATE` | Retiro iniciado, pendiente de confirmación | Sin cambios | N/A |
| 2 | `PAYMENT_PENDING` | Confirmado por usuario, esperando aprobación | Descontado | Aprobar/Rechazar |
| 1 | `PAYMENT_SUCCESS` | Aprobado por admin | Permanece descontado | Procesar pago externo |
| 3 | `PAYMENT_REJECT` | Rechazado por admin | **Reembolsado** | N/A |

---

## 10. Configuración y Personalización

### 10.1 Fórmula de Cálculo de Comisiones (Retiros)

```
┌─────────────────────────────────────────────────────────────────┐
│           CÁLCULO DE COMISIONES EN RETIROS                      │
│     (DIFERENTE A DEPÓSITOS - SE RESTA EN LUGAR DE SUMAR)        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Datos de entrada:                                              │
│  ─────────────────                                              │
│  • amount          = Monto solicitado por usuario               │
│  • fixed_charge    = Comisión fija del método                   │
│  • percent_charge  = Comisión porcentual del método             │
│  • rate            = Tasa de cambio a moneda destino            │
│                                                                 │
│  Fórmulas:                                                      │
│  ─────────                                                      │
│                                                                 │
│  charge = fixed_charge + (amount × percent_charge / 100)        │
│                                                                 │
│  afterCharge = amount - charge   ← SE RESTA (no se suma)        │
│                                                                 │
│  finalAmount = afterCharge × rate                               │
│                                                                 │
│  Ejemplo:                                                       │
│  ────────                                                       │
│  • amount = $100.00                                             │
│  • fixed_charge = $1.50                                         │
│  • percent_charge = 1.5%                                        │
│  • rate = 1.10 (USD → EUR)                                      │
│                                                                 │
│  charge = $1.50 + ($100.00 × 1.5 / 100)                         │
│         = $1.50 + $1.50                                         │
│         = $3.00                                                 │
│                                                                 │
│  afterCharge = $100.00 - $3.00                                  │
│              = $97.00                                           │
│                                                                 │
│  finalAmount = $97.00 × 1.10                                    │
│              = €106.70                                          │
│                                                                 │
│  ✅ Usuario solicita: $100.00                                   │
│  ✅ Se descuenta de balance: $100.00                            │
│  ✅ Comisión: $3.00                                             │
│  ✅ Usuario recibe: €106.70                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 Comparación: Depósitos vs Retiros

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    DEPÓSITOS vs RETIROS - DIFERENCIAS CLAVE                     │
├───────────────────────────┬───────────────────────────┬─────────────────────────┤
│        Concepto           │        DEPÓSITOS          │        RETIROS          │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Fórmula de comisión       │ total = amount + charge   │ total = amount - charge │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Procesamiento             │ Automático o Manual       │ Siempre Manual          │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Balance afectado          │ Al confirmar pago         │ Al enviar solicitud     │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Dirección de balance      │ balance += amount         │ balance -= amount       │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Reembolso automático      │ No aplica                 │ Sí (al rechazar)        │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Verificación KYC          │ Opcional                  │ Puede ser requerida     │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Verificación 2FA          │ No                        │ Opcional                │
├───────────────────────────┼───────────────────────────┼─────────────────────────┤
│ Gateways externos         │ 30+ automáticos           │ Ninguno (todo manual)   │
└───────────────────────────┴───────────────────────────┴─────────────────────────┘
```

### 10.3 Agregar un Nuevo Método de Retiro

```
Admin → Withdraw Methods → Add New

┌─────────────────────────────────────────────────────────────────┐
│                   NEW WITHDRAW METHOD                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Method Name:     [Bank Transfer__________________________]     │
│                                                                 │
│  Currency:        [USD_____]                                    │
│                                                                 │
│  Rate:            [1.00___] (1 Site Currency = X Method Curr.)  │
│                                                                 │
│  Minimum:         [50.00__]                                     │
│  Maximum:         [10000.0]                                     │
│                                                                 │
│  Fixed Charge:    [5.00___]                                     │
│  Percent Charge:  [1.5____] %                                   │
│                                                                 │
│  Image:           [Choose File]                                 │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  Instructions (shown to user):                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Withdrawals will be processed within 24-48 hours.        │   │
│  │ Please ensure your bank details are correct.             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  Required Information from User:                                │
│                                                                 │
│  [+ Add Field]                                                  │
│                                                                 │
│  1. [Bank Name         ] Type: [Text     ▼] Required: [✓]      │
│  2. [Account Number    ] Type: [Text     ▼] Required: [✓]      │
│  3. [Routing Number    ] Type: [Text     ▼] Required: [✓]      │
│  4. [Account Holder    ] Type: [Text     ▼] Required: [✓]      │
│  5. [SWIFT Code        ] Type: [Text     ▼] Required: [○]      │
│                                                                 │
│  [              💾 Create Method              ]                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Apéndice A: Notificaciones del Sistema

| Template | Evento | Destinatario |
|----------|--------|--------------|
| `WITHDRAW_REQUEST` | Usuario envía solicitud | Usuario |
| `WITHDRAW_APPROVE` | Admin aprueba retiro | Usuario |
| `WITHDRAW_REJECT` | Admin rechaza retiro | Usuario |

---

## Apéndice B: Transacciones Generadas

### Al enviar solicitud de retiro:
```php
Transaction {
    user_id: $user->id,
    amount: $withdraw->amount,
    post_balance: $user->balance,  // Balance después del descuento
    charge: $withdraw->charge,
    trx_type: '-',                 // NEGATIVO
    details: 'Withdraw request via [método]',
    trx: $withdraw->trx,
    remark: 'withdraw'
}
```

### Al rechazar retiro (reembolso):
```php
Transaction {
    user_id: $withdraw->user_id,
    amount: $withdraw->amount,
    post_balance: $user->balance,  // Balance después del reembolso
    charge: 0,
    trx_type: '+',                 // POSITIVO
    details: 'Refunded for withdrawal rejection',
    trx: $withdraw->trx,
    remark: 'withdraw_reject'
}
```

---

## Apéndice C: Middleware de Verificación

El sistema utiliza el middleware `kyc` para las rutas de retiro, lo que significa que los usuarios pueden requerir verificación KYC antes de poder retirar fondos. Esto se configura en:

```
Admin → System Configuration → KYC Verification
```

---

**Documento generado para:** RaffleLab - Sistema de Retiros  
**Versión del documento:** 1.0  
**Última actualización:** Febrero 2026
